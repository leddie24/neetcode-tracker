version: '3.8'

services:
  neetcode-tracker:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    container_name: neetcode-tracker
    restart: unless-stopped
